# ============================================================
# نموذج متغيرات بيئة الواجهة — انسخه إلى .env عند الحاجة
#   cp frontend/.env.example frontend/.env
# ============================================================

# اتركه فارغاً في التطوير (يُستخدم بروكسي Vite للمسار /api)
# VITE_API_URL=
#
# في الإنتاج (Vercel) اضبطه على رابط الباك إند بدون /api
# VITE_API_URL=https://beauty-hub-api.onrender.com
